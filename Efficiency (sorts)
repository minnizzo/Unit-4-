import time
 #Comparing searches
    target = input("Enter vendor name to search for: ")

    #Linear Search
    begin = time.perf_counter()
    linearSearchUnsorted(vendor_list, target)
    timeA = time.perf_counter() - begin

    #Binary Search
    begin = time.perf_counter()
    binarySearch(sorted_list, target)
    timeB = time.perf_counter() - begin

    print("Search Efficiency")
    print("-----------------")
    print(f"Linear search: {timeA:.6f} seconds.")
    print(f"Binary search: {timeB:.6f} seconds.")

