hotdogs = []
with open("Hotdogs.txt","r") as file:
    data = file.read()
    #.read allows the code to output the data which is stored in Hotdogs.txt
def data(hotdogs):
    vendor_list = []
    try:
        hotdogs = open("Hotdogs.txt","r")
        
        for line in file:
            strip = line.strip()
            columns = strip.split(",")
            #This allows you to split the information seperately.
            
            if len(columns) == 7:
                records = {
                "id": columns[0],
                "name": columns[1],
                "date": columns[2],
                "vegan": int(columns[3]),
                "meat": int(columns[4]),
                "onions": float(columns[5]),
                "ketchup": float(columns[6])
            }
            vendor_list.append(records)
            #.append will add everything that is stored in records to be stored into a variable called 'vendor_list'
        file.close()
        print("Loaded",vendor_list,"vendors.")
        return vendor_list
    
    except FileNotFoundError:
        print("Error detected: The file Hotdogs.txt was not found.")
        return []
    #Exception handling will allow the code to run without it crashing. In this case, the possible error that could happen is 'FileNotFoundError'.


